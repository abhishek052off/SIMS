@{
    ViewData["Title"] = "Add Course";
}

@using SIMSWeb.ConstantsAndUtilities;
@using SIMSWeb.Models.Course

@model AddCourseVM;
<form method="post" asp-controller="Course" asp-action="AddCourses">
    <div class="p-3 pt-4 gap-2">
        <div class="row">
            <h2>Add Course</h2>
            <hr />
        </div>
        <div asp-validation-summary="ModelOnly"></div>
        <div class="mb-3">
            <label>Name</label>
            <input asp-for="@Model.CourseModel.Name" name="Name" type="text" class="form-control" />
            <span asp-validation-for="@Model.CourseModel.Name" class="text-danger" />
        </div>
        <div class="row mb-3">
            <div class="col-1">
                <label class="form-label">Active</label>
            </div>
            <div class="col-1">
                <input asp-for="@Model.CourseModel.IsActive" name="IsActive"  type="checkbox" checked class="form-check" />
            </div>
        </div>
        <div class="mb-3">
            <label>Teachers</label>
            <select asp-items="@(new SelectList(Model.TeacherListModel, "Id","Name"))" name="TeacherId" class="form-control">
                <option value="select">Select Teachers</option>
            </select>
        </div>
        <div class="row">
            <div class="col-6">
                <button type="submit" class="btn btn-primary">Add</button>
                <button asp-controller="Users" asp-action="ManageCourses" class="btn btn-outline-primary">Cancel</button>
            </div>
        </div>
    </div>
</form>


@section Scripts
{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}

